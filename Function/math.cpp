
// library = math functions
#include <iostream>
#include <cmath> // Need to include a <cmath>  Header for a library
using namespace std;

int main(){
  int a;
  cout<<"Enter the 1st number ";
  cin>>a;
  int b;
  cout<<"Enter the 2nd number ";
  cin>>b;
  // cout<<sqrt(a);
  // cout<<cbrt(a);
  // cout<<min(a,b); //note min and max function are work without the <cmath> Header
  // cout<<max(a,b);
  cout<<pow(a,b);
}
